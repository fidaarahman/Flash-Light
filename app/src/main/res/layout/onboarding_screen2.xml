<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/onboard1_background"
    android:padding="16dp">

    <!-- Onboarding Image -->
    <FrameLayout
        android:id="@+id/fl_main"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:background="#161616"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="100dp">

        <!-- Phone Image -->
        <ImageView
            android:id="@+id/iv_phone"
            android:layout_width="201dp"
            android:layout_height="209dp"
            android:layout_marginStart="@dimen/_10sdp"
            android:layout_marginTop="@dimen/_50sdp"
            android:scaleType="fitCenter"
            android:src="@drawable/second_phone" />

        <!-- Flashlight Effect (Overlay) -->
        <ImageView
            android:id="@+id/iv_flashlight"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:src="@drawable/second_phone1"
            android:scaleType="fitCenter"
            android:layout_marginStart="@dimen/_65sdp"
            android:layout_marginBottom="@dimen/_100sdp"/>
    </FrameLayout>

    <!-- Subtitle -->
    <TextView
        android:id="@+id/tv_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
<<<<<<< HEAD
        android:text="@string/find_anything_in_dark"
=======
        android:text="Find anything in dark"
>>>>>>> 60f676ca937c5aa0453a2918f1d3009a6b80c889
        android:textSize="16sp"
        android:fontFamily="@font/poppin_regular"
        android:textColor="@android:color/white"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/fl_main"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp"/>



</androidx.constraintlayout.widget.ConstraintLayout>
